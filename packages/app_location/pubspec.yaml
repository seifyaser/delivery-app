name: app_location
description: A robust, production-ready, framework-agnostic location infrastructure package.
version: 1.0.0
publish_to: 'none' # Internal package

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.4
  geocoding: ^3.0.0
  location: ^6.0.2
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
